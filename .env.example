# API Configuration
PORT=3333
NODE_ENV=development
# Frontend Configuration (used by Docker during the build process)
# The /api path is handled by the Nginx proxy in the production setup.
VITE_API_URL=http://localhost:3333/api
